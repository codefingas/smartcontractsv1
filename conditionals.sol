pragma solidity ^0.6.0;

/**
    conditionals in solidity are similar to the types in other languages
        - but there is on switch statement or go to.
 */

 contract SimpleContract {
     // if statement
     if (a == 2){
         // run code if condition met
     } else {
         // else run code
     }

     // while 
     while (a >= 2) { // check condition first
         // run code if condition met
     }

     do { // run this code until condition is no longer true.
         // do this code first 
     } while (a >= 2)


    // for loop

    for (uint i = 0; i > = 50; i++){
        // run code while condition is met
    }

 }